<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.drunktester.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.drunktester.AccelerometerActivity" />
        <activity android:name="com.example.drunktester.RecordingAccelerometerActivity" />
        <activity android:name="com.example.drunktester.VoiceRecordActivity" />
        <activity android:name="com.example.drunktester.RecordingVoiceActivity" />
        <activity android:name="com.example.drunktester.AnalyzeActivity" />
    </application>

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>